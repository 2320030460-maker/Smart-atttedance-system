# Smart-atttedance-system
💡 Smart Attendance System (Same Type Project)

This is a Face Recognition Based Attendance System (very similar to your repo).

👉 These systems typically:

Capture face via camera
Compare with stored images
Automatically mark attendance
Save into CSV/database
🔧 Tech Stack (simple & effective)
Python
OpenCV
face_recognition library
CSV / SQLite
📁 Project Structure
smart-attendance/
│── dataset/              # Stored face images
│── attendance.csv       # Attendance record
│── main.py              # Main program
│── encode_faces.py      # Face encoding
│── requirements.txt
